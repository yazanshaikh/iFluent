/**
 * @ifluent/shared — barrel export
 * كل الـ apps تستورد منه مباشرة
 *
 * Usage:
 *   import { Colors, Spacing, useResponsive } from '@ifluent/shared';
 *   import type { SiteSettings, UserRole }    from '@ifluent/shared';
 */
export * from './constants';
export * from './hooks';
export * from './types';
