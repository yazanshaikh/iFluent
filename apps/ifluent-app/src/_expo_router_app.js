/**
 * Shim used only when expo/AppEntry.js is loaded as the entry in a monorepo.
 * See apps/frontend-student/src/_expo_router_app.js for full explanation.
 */
export { App as default } from 'expo-router/build/qualified-entry';
